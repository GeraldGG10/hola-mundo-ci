name: Notificacion Ntfy

on:
  push:
    branches:
      - main

jobs:
  notificar:
    runs-on: ubuntu-latest

    steps:
      - name: Enviar notificacion a ntfy.sh
        run: |
          curl -d "Gerald Manuel Gomera - Matricula: 2024-0044" \
               https://ntfy.sh/devops-itla
